# Arabic Localization Protocols
- **Language:** Strict Modern Standard Arabic (الفصحى).
- **Slugs:** MUST use `app/Helpers/SlugHelper.php`.
- **Layout:** RTL-First (`dir="rtl"`). Use `ms-auto`, `pe-4` (Logical Properties).
